# Sample Output

This folder is the default export destination, but the workspace is kept clean between engineering passes.

Typical output:

- `sample_output/generated/<estimate_name>.esx`
- `sample_output/generated/<estimate_name>.esx.xml`
- `sample_output/generated/<estimate_name>.canonical.json`

Generated artifacts are runtime output only and are not required to launch the app.
